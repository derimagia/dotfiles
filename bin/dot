#!/bin/sh
#
# dot
#
# `dot` handles installation, updates, things like that. Run it periodically
# to make sure you're on the latest and greatest.

# Set OS X defaults
$DOTFILES/apps/osx.sh 2>&1

# Install homebrew packages
$DOTFILES/apps/homebrew.sh 2>&1

# Install python packages
$DOTFILES/apps/python.sh 2>&1

# Install node packages
$DOTFILES/apps/node.sh 2>&1

# Install ruby packages
$DOTFILES/apps/ruby.sh 2>&1

# Install idea packages
$DOTFILES/apps/idea/idea.sh 2>&1

# Install OSX Settings
$DOTFILES/apps/osx.sh 2>&1
