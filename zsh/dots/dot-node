#!/usr/bin/env zsh

(( $+commands[node] )) || exit 34

ink -l -c green -- "[node] Installing npm Packages"
npm install -g \
    npm \
    babel-core \
    bcat \
    ctrace \
    gulp \
    speed-test \
    yo
